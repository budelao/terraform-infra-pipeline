name: "Terraform Workflow - Passo 01"

on:
  workflow_call:

jobs:
  checkout_code:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

